#ifndef GET_TIMESTAMP_H
#define GET_TIMESTAMP_H

// Returns a pointer to a static string containing the current timestamp.
// Format: YYYY-MM-DD HH:MM:SS
const char* get_timestamp();

#endif // GET_TIMESTAMP_H